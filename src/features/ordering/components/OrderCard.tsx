import React from 'react';
import { Pressable, Text, View } from 'react-native';
import { RefundControls } from '../../payments/components/RefundControls';
import { styles } from '../../payments/styles';
import { Order, OrderStatus } from '../types';
import { formatGbp } from '../validation';
const next: Partial<Record<OrderStatus, OrderStatus>> = { accepted: 'preparing', preparing: 'ready', ready: 'completed' };
export function OrderCard({order,busy,canManage,onTransition,onRefund}:{order:Order;busy:boolean;canManage:boolean;onTransition:(status:OrderStatus,reason?:string)=>void;onRefund?:(amountPence:number,reason:string)=>void}){const refundable=order.totalPence-order.refundedPence;return <View style={styles.card}><View style={styles.row}><Text style={styles.cardTitle}>Order {order.id.slice(0,8)}</Text><Text style={styles.status}>{order.status.replace('_',' ')}</Text></View>{order.items.map(item=><Text key={item.id} style={styles.item}>{item.quantity} × {item.name}</Text>)}<View style={styles.row}><Text style={styles.body}>{new Date(order.createdAt).toLocaleString('en-GB')}</Text><Text style={styles.amount}>{formatGbp(order.totalPence)}</Text></View>{order.cancellationReason?<Text style={styles.error}>{order.cancellationReason}</Text>:null}{canManage&&order.status==='needs_confirmation'?<View style={styles.row}><Pressable disabled={busy} onPress={()=>onTransition('accepted')} style={[styles.button,{flex:1},busy&&styles.disabled]}><Text style={styles.buttonText}>Accept</Text></Pressable><Pressable disabled={busy} onPress={()=>onTransition('refund_pending','Rejected by shop')} style={[styles.button,styles.buttonDanger,{flex:1},busy&&styles.disabled]}><Text style={styles.buttonText}>Reject & refund</Text></Pressable></View>:null}{canManage&&next[order.status]?<Pressable disabled={busy} onPress={()=>onTransition(next[order.status]!)} style={[styles.button,busy&&styles.disabled]}><Text style={styles.buttonText}>Mark {next[order.status]!.replace('_',' ')}</Text></Pressable>:null}{onRefund&&refundable>0&&['paid','partially_refunded'].includes(order.paymentStatus)?<RefundControls availablePence={refundable} busy={busy} onSubmit={onRefund}/>:null}</View>}
