import React from 'react';
import { Alert, Pressable, Text, View } from 'react-native';
import { formatGbp } from '../../ordering/validation';
import { styles } from '../../payments/styles';
import { useTill } from '../hooks';
export function TillScreen({businessId}:{businessId:string}){const till=useTill(businessId);const run=(action:()=>Promise<unknown>)=>void action().catch(error=>Alert.alert('Till',error instanceof Error?error.message:'Please try again.'));return <View style={{gap:14}}><View style={styles.card}><Text style={styles.cardTitle}>Reader</Text><Text style={styles.body}>{till.message}</Text><View style={styles.row}><Pressable disabled={till.busy} onPress={()=>run(()=>till.discover(true))} style={[styles.button,styles.buttonSecondary,{flex:1},till.busy&&styles.disabled]}><Text style={styles.buttonSecondaryText}>Simulator</Text></Pressable><Pressable disabled={till.busy} onPress={()=>run(()=>till.discover(false))} style={[styles.button,styles.buttonSecondary,{flex:1},till.busy&&styles.disabled]}><Text style={styles.buttonSecondaryText}>Find WisePad 3</Text></Pressable></View>{till.readers.map((reader,index)=><Pressable key={reader.serialNumber} disabled={till.busy} onPress={()=>run(()=>till.connect(index))} style={styles.button}><Text style={styles.buttonText}>{reader.label??reader.deviceType} · {reader.serialNumber}</Text></Pressable>)}</View><View style={styles.card}><Text style={styles.cardTitle}>Available menu</Text>{till.items.map(item=><Pressable key={item.id} onPress={()=>till.add(item)} style={styles.row}><Text style={styles.item}>{item.name}</Text><Text style={styles.amount}>{formatGbp(Math.round(item.price*100))} +</Text></Pressable>)}</View><View style={styles.card}><Text style={styles.cardTitle}>Basket</Text>{!till.basket.length?<Text style={styles.body}>Add items from the menu.</Text>:till.basket.map(line=><Pressable key={line.menuItemId} onPress={()=>till.remove(line.menuItemId)} style={styles.row}><Text style={styles.item}>{line.quantity} × {line.name}</Text><Text style={styles.body}>Tap to remove one</Text></Pressable>)}<View style={styles.row}><Text style={styles.cardTitle}>Total</Text><Text style={styles.amount}>{formatGbp(till.totalPence)}</Text></View><Pressable disabled={till.busy||!till.basket.length||!till.connectedReader} onPress={()=>run(till.charge)} style={[styles.button,(till.busy||!till.basket.length||!till.connectedReader)&&styles.disabled]}><Text style={styles.buttonText}>Take card payment</Text></Pressable></View></View>}
